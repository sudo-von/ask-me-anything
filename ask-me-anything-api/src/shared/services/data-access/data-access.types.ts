import { IService } from "@services/services.types";

/**
 * Represents a data access service that extends the basic functionality of a general service.
 */
export interface IDataAccessService extends IService { };
